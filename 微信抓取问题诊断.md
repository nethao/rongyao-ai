# 微信公众号抓取问题诊断与解决方案

## 问题描述

微信公众号文章之前可以正常抓取，现在抓取失败。

## 根本原因

经过诊断，发现以下问题：

### 1. **网络连接超时**
- Playwright访问微信公众号时，页面加载超时（30秒）
- requests方式也出现连接超时
- DNS解析正常，但TCP连接建立失败或响应缓慢

### 2. **微信反爬机制升级**
- 微信检测到自动化访问，返回验证页面
- 页面内容包含"环境异常"或"安全验证"提示
- 服务器IP可能被微信列入黑名单

### 3. **Docker环境限制**
- 容器内网络环境与真实浏览器环境差异大
- Playwright的反检测措施在容器中效果有限

## 已实施的改进

### 1. **增强Playwright反检测能力**
```python
- 添加更多浏览器启动参数（--disable-blink-features等）
- 注入JavaScript脚本隐藏webdriver特征
- 设置真实的浏览器指纹（viewport、locale、timezone）
- 添加完整的HTTP请求头
```

### 2. **优化超时和重试策略**
```python
- 增加页面加载超时到45秒
- 使用'load'而不是'networkidle'等待策略
- 添加3次重试机制，每次间隔递增
- 超时后仍尝试解析已加载的内容
```

### 3. **改进requests备用方案**
```python
- 增加超时时间到30秒
- 添加更完整的请求头
- 检测验证页面并提前返回
```

### 4. **降级方案**
当抓取失败时，系统会：
- 创建包含原文链接的草稿
- 提示用户手动打开链接复制内容
- 保留文章URL供后续处理

## 可能的解决方案

### 短期方案（已实施）

✅ **1. 优化现有代码**
- 增加超时时间和重试次数
- 改进反检测措施
- 提供友好的降级体验

### 中期方案（推荐）

🔧 **2. 使用代理IP池**
```python
# 在web_fetcher.py中添加代理支持
proxies = {
    'http': 'http://proxy-server:port',
    'https': 'http://proxy-server:port'
}
response = self.session.get(url, proxies=proxies)
```

🔧 **3. 降低请求频率**
- 在邮件任务中添加延迟
- 避免短时间内大量请求
- 使用队列控制并发数

### 长期方案

🚀 **4. 使用第三方API服务**
- 考虑使用专业的网页抓取API（如ScrapingBee、Bright Data）
- 这些服务提供IP轮换和反爬绕过

🚀 **5. 改用微信公众号API**
- 如果有公众号授权，使用官方API获取文章
- 需要公众号管理员授权

🚀 **6. 手动提交流程优化**
- 开发浏览器插件，一键提取文章内容
- 提供移动端小程序，方便用户提交

## 测试方法

### 1. 测试单个链接
```bash
cd /home/nethao/rongyao-ai
docker exec backend python -c "
from app.services.web_fetcher import WebFetcher
fetcher = WebFetcher()
result = fetcher.fetch_weixin_article('https://mp.weixin.qq.com/s/xxxxx')
print('标题:', result[0])
print('内容长度:', len(result[1] or ''))
print('图片数:', len(result[2]))
"
```

### 2. 查看实时日志
```bash
docker-compose logs -f celery_worker | grep -i "weixin\|微信"
```

### 3. 检查网络连接
```bash
docker exec backend python -c "
import requests
resp = requests.get('https://mp.weixin.qq.com/', timeout=10)
print('状态码:', resp.status_code)
print('是否验证页面:', '验证' in resp.text)
"
```

## 当前状态

- ✅ 代码已优化，增加了重试和更好的错误处理
- ✅ 降级方案已就绪，抓取失败时提供链接
- ⚠️ 网络连接问题仍存在，需要进一步排查
- ⚠️ 可能需要使用代理IP或第三方服务

## 建议

1. **立即行动**：
   - 重启服务后测试几个链接
   - 观察是否有改善
   - 如果仍然超时，考虑检查服务器网络配置

2. **短期内**：
   - 如果问题持续，考虑使用代理IP
   - 或者暂时使用手动提交流程

3. **长期规划**：
   - 评估第三方抓取服务的成本
   - 或开发浏览器插件简化手动提交

## 相关文件

- `/home/nethao/rongyao-ai/backend/app/services/web_fetcher.py` - 抓取服务
- `/home/nethao/rongyao-ai/backend/app/tasks/email_tasks.py` - 邮件处理任务
- `/home/nethao/rongyao-ai/backend/app/services/email_parser.py` - 邮件解析

## 更新日期

2026-03-02
